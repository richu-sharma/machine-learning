# Clustering Algorithms

This folder contains implementations of clustering algorithms used in **unsupervised machine learning**.

Algorithms included:

* K-Means Clustering
* Hierarchical Clustering
* DBSCAN

Clustering is used to group similar data points together without labeled outputs.
